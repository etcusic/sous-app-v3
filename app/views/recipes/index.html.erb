<h1><%= @recipes.first.user.name %></h1>

<ul>
    <% @recipes.each do |recipe| %>
        <li><%= link_to "#{recipe.name}", user_recipe_path(recipe.user, recipe) %> - <%= recipe.portions %> servings</li>
    <% end %>
</ul>